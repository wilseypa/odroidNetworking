commit 691fda314f1dab64d93c8b6d3d9d527a9d47359e
Author: Yevgeny Petrilin <yevgenyp@mellanox.co.il>
Date:   Wed Aug 26 15:40:50 2009 +0300

    mlx4_en: Settign dev->perm_addr field
    
    Signed-off-by: Yevgeny Petrilin <yevgenyp@mellanox.co.il>

diff --git b/drivers/net/mlx4/en_netdev.c a/drivers/net/mlx4/en_netdev.c
index ded56b9..0e9fbbc 100644
--- b/drivers/net/mlx4/en_netdev.c
+++ a/drivers/net/mlx4/en_netdev.c
@@ -1039,10 +1039,9 @@ int mlx4_en_init_netdev(struct mlx4_en_dev *mdev, int port,
 
 	/* Set defualt MAC */
 	dev->addr_len = ETH_ALEN;
-	for (i = 0; i < ETH_ALEN; i++) {
-		dev->dev_addr[ETH_ALEN - 1 - i] = (u8) (priv->mac >> (8 * i));
-		dev->perm_addr[ETH_ALEN - 1 - i] = (u8) (priv->mac >> (8 * i));
-	}
+	for (i = 0; i < ETH_ALEN; i++)
+		dev->dev_addr[ETH_ALEN - 1 - i] =
+		(u8) (priv->mac >> (8 * i));
 
 	/*
 	 * Set driver features
